
  # Convert to React Tailwind

  This is a code bundle for Convert to React Tailwind. The original project is available at https://www.figma.com/design/ELjvHzLZX60Z7fWflfgd78/Convert-to-React-Tailwind.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  